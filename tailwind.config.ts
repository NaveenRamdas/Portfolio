import type { Config } from "tailwindcss";

// This project uses Tailwind CSS v4.
// All theme configuration is in src/app/globals.css via @theme block.
// This file is kept for IDE tooling compatibility only and is not loaded by the build.
const config: Config = {
  content: [],
};

export default config;
